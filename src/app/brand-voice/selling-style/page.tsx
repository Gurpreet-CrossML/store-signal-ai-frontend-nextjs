import { Suspense } from "react";

export const metadata = {
  title: "Selling Style",
};

export default function Page() {
  return (
    <Suspense fallback={null}>
      <></>
    </Suspense>
  );
}
